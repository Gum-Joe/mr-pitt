// Find the maximum value in a list of numbers
var num = [];
numb = function(a) {
    var n = 30
    for (var i = 0; i < n; i++) {
        writeln("n = "+i);
        num[i] = i;
    }
}
numb()
writeln(num)
pri = function(nu) {
    for (var j = 0; j < nu.length; j++) {
        writeln("n = "+i);
        num[i] = i;
    }
}
